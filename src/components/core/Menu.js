import React, { Component } from 'react';

class Menu extends Component {
    render() {
        return (
            <nav className={this.props.classes}>
                <h1>Menu</h1>
            </nav>
        );
    }
}

export default Menu;
